#include <stdio.h>

int main() {
  int SPL$ = 10;

  printf("%d\n", SPL$);
int a = (float)15/4;
float b = a++*a--;
int c = (a>b || a==1+2)*2;
float d = a/c;
 
  printf("a = %d, b = %f, c = %d, d = %f\n", a, b, c, d);
    return 0;
}